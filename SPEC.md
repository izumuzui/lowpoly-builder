# Low Poly Creator — 仕様書

写真から PS1（初代PlayStation）風のローポリゴン3Dキャラクターを生成し、Blenderで使える `.glb` として書き出すブラウザアプリ。

---

## 1. 目的とゴール

顔写真をアップロードすると、あらかじめ用意されたカクカクの体型モデルに顔テクスチャが自動で貼られ、リグ（骨）付きの3Dモデルとして書き出される。

生成物の想定用途:

- SNSのアイコン（プレビュー画面からPNG書き出し）
- Music Video（`.glb` をBlenderにインポートし、Mixamoのアニメーションで動かす）

### 達成条件

1. 写真1枚のアップロードから30秒以内に `.glb` がダウンロードできる
2. 書き出した `.glb` をBlenderにインポートすると、メッシュ・テクスチャ・ボーンがすべて正しく読み込まれる
3. ボーン名がMixamo標準と一致し、Mixamoのアニメーションをリターゲット作業なしで適用できる
4. 体型の追加が、JSONファイルを1枚足すだけで完了する

---

## 2. 既存サービス調査と差別化

2026年8月時点の調査結果。既存サービスは3カテゴリに分かれ、いずれも要件を満たさない。

| カテゴリ | 例 | 満たさない理由 |
|---|---|---|
| 2D画像フィルタ | Glima.ai, PersonaPixel, PS2 Filter | 3Dモデルが出力されない。画像のみ |
| AI 3D生成 | Tripo AI, Meshy, Hyper3D | メッシュ・トポロジー・リグが毎回変わる。体型の作り分けと再現性がない |
| フォトリアル系アバター | Avatar SDK / MetaPerson, Avaturn | リアル寄りの中ポリ。PS1の質感がない。Ready Player Meは2026年1月31日に終了 |

### 本アプリの立ち位置

**固定のローポリ・ベースメッシュ群 × 写真そのままの顔テクスチャ × glTF書き出し**

この組み合わせは既存にない。メッシュをAIに作らせず自前で持つことで、ポリゴン数・UV・リグをすべて制御下に置く。

### AI推論を使わない

顔テクスチャは「写真を切り抜いて位置合わせして貼る」だけで生成する。生成AIによる描き直しは行わない。

理由:

- **意図した画作り** — リアルな顔写真がカクカクの体に乗っている違和感（いわゆる「バタ臭いPS1顔」）が本アプリの狙いであり、AIで整えるとこの味が消える
- **テクスチャを128px以下に落とすため、写真の粗や切り抜きの粗が「味」に転化する**
- サーバー・APIキー・課金が一切不要になり、静的ホスティングで完結する
- 写真がブラウザ外に出ない（プライバシー）

---

## 3. 技術構成

### 方針: ビルドツールなし

importmap + ESモジュール + CDN。npm / node_modules / バンドラを使わない。

| 項目 | 選択 | 理由 |
|---|---|---|
| モジュール読み込み | importmap（Web標準） | ブラウザが直接ESモジュールを解決できる。ビルド工程が不要 |
| 3D | three.js r184 | WebGLの事実上の標準。GLTFExporterを同梱 |
| 顔検出 | MediaPipe Tasks Vision 1.0.1 (FaceDetector) | ブラウザ内WASMで動作。サーバー不要 |
| UIフレームワーク | なし（素のDOM） | 画面の大半がcanvas 1枚。DOMの複雑さがなくReactの利点が効かない |
| ホスティング | GitHub Pages | 静的ファイルのみ。`git push` でデプロイ |

### 将来のVite移行に備えた制約

コードは以下を守り、いつでもバンドラ配下に移せる状態を保つ。

- モジュール参照は必ずベアインポート（`import * as THREE from 'three'`）で書く。CDNのURLをソース中に直書きしない
- URL指定はimportmapに集約する
- グローバル変数への依存を作らない

移行が必要になった場合は `npm install three` を実行し、`index.html` のimportmapを削除するだけで完了する。ソースコードの変更は発生しない。

### 依存の固定

CDNのバージョン間でモジュールの置き場所が変わる事故が実際に起きたため（r184で `Timer` がaddonsからコアへ移動、MediaPipeが `0.10.x` から `1.0.1` へ）、依存は取り込む方針とする。ただし全部は取り込まない。

| 依存 | 扱い | 理由 |
|---|---|---|
| three.js（コア + OrbitControls + GLTFExporter） | `vendor/` に取り込む（2.2MB） | サイズが現実的で、実際にバージョン差異で壊れた箇所 |
| MediaPipe | CDNのまま、バージョン固定 | WASMだけで11.5MBあり、リポジトリに入れると重すぎる |

MediaPipeは**静的importにせず、写真が選ばれてから動的に読み込む**。体型を見るだけの利用では外部通信が一切発生しない。

> `three.module.js` は自己完結ではなく同階層の `three.core.js` を読む。取り込む際は両方を並べて置く必要がある。更新は `vendor/update.sh` で行う。

---

## 4. 機能要件

### 4.1 写真入力

- 入力: ファイル選択またはドラッグ&ドロップ。JPEG / PNG / WebP
- 顔が検出できない場合はその旨を表示し、モデル生成は行わない
- 複数の顔が検出された場合は、画像内で最も大きい顔を採用する
- 画像はブラウザ外に送信しない

### 4.2 顔テクスチャ生成

MediaPipe FaceLandmarker で478点のランドマークを取得し、以下の手順で顔領域をテクスチャアトラスへ転写する。

**位置合わせ（相似変換 — 回転・拡大縮小・平行移動のみ）**

1. 左右の目の中心座標を求める（ランドマーク33および263を基準）
2. 両目を結ぶ線が水平になるよう回転角を決める
3. 両目間距離が顔領域の幅の42%になるよう倍率を決める
4. 両目の中点が顔領域の (50%, 42%) に来るよう平行移動する

透視変形やランドマーク単位のワープは行わない。素朴なほど狙いの質感に近い。

**PS1風の減色処理**

1. 上記の変換をかけて128×128のcanvasへ描画（この時点では平滑化を有効にし、ジャギーを抑える）
2. **外周を楕円状に肌色へ溶かす** — 顔テクスチャは正面だけでなく斜め前の面にも回り込むため、切り抜かないと写真の背景や耳が頭の側面に貼り付く
3. 各チャンネルを5bit（32段階）に量子化する — 初代PlayStationのフレームバッファは15bitカラーであり、これを再現する
4. アトラスの `face` 領域へ転写する

頭のUVは軸に沿った平面投影とし、**幅と高さの大きい方で正規化する**。テクスチャ領域が正方形なのに対し頭は縦長のため、そのまま貼ると顔が縦に伸びる。

顔検出には478点を返す `FaceLandmarker`（約3.7MB）ではなく、6点を返す `FaceDetector`（BlazeFace short-range、約224KB）を使う。位置合わせに必要なのは両目の座標だけであり、ランドマークは過剰。

### 4.3 体型システム

体型は**JSONのスペックからプロシージャルにメッシュを生成する**。完成済みの `.glb` を読み込む方式は採らない。

理由: PS1のキャラクターは実質「テーパーのかかった箱の集合」であり、パラメータ記述と相性がよい。完成品メッシュ方式ではUV配置とボーン名を毎回手作業で合わせる必要があり、「あとから体型を増やせる」という要件が実質的に破綻する。

- 体型の追加 = `bodies/` にJSONを1枚追加し、`bodies/index.json` に登録する
- 初期搭載: 標準 / がっしり / 細身 / 頭身低め の4種

パーツの形状は2種類。

| shape | 用途 |
|---|---|
| `box` | 手・足。角張っていて問題ない部位 |
| `rings` | 頭・胴・腕・脚。断面（リング）を積み重ねた多角柱 |

`rings` は輪郭を `profile` で、断面の角数を `sides` で、積み上げる向きを `axis` で指定する。腕はTポーズでX方向に伸びるため `axis: "x"` を使う。

断面は面の1枚がちょうど `axis` に垂直な基準方向（`axis: "y"` なら +Z）を向くよう角度を半ステップずらす。頭ではこれにより顔の中心が安定し、その左右の面へ顔テクスチャが回り込む。

### 細かさ

`sides` と `profile` の行数に倍率を掛けて、メッシュの細かさを4段階で切り替えられる。**輪郭（profile）そのものは変えないためシルエットは保たれる。**

**細かさの主眼は分割ではなく、段階ごとにパーツそのものが増えること。** 分割だけを上げても情報量は増えず、ただ重くなるだけになる。パーツ側の `detail` フィールドで制御する。

| 段階 | 追加されるパーツ | 角数 | リング | 標準体型のポリゴン数 |
|---|---|---|---|---|
| 粗い | — | ×0.7 | ×1 | 652 |
| 標準 | 肩 | ×1 | ×1 | 976 |
| 細かい | ＋耳・親指・かかと | ×1.25 | ×1 | 1,340 |
| とても細かい | ＋指3本 | ×1.6 | ×2 | 2,944 |

PS1期の実機水準は300〜800ポリゴン程度であり、**「細かい」以上は当時のハードでは動かない密度**である。PS1らしさを取るか滑らかさを取るかの選択になるため、既定は「標準」とし、利用者が選べるようにしている。

### 4.4 リグ（ボーン）

Mixamo標準の命名・階層でスケルトンを生成する。

```
mixamorig:Hips
├── mixamorig:Spine → Spine1 → Spine2
│   ├── mixamorig:Neck → Head → HeadTop_End
│   ├── mixamorig:LeftShoulder → LeftArm → LeftForeArm → LeftHand
│   └── mixamorig:RightShoulder → RightArm → RightForeArm → RightHand
├── mixamorig:LeftUpLeg → LeftLeg → LeftFoot → LeftToeBase → LeftToe_End
└── mixamorig:RightUpLeg → RightLeg → RightFoot → RightToeBase → RightToe_End
```

**スキニングは1頂点1ボーンの剛体ウェイト**とする。頂点を複数ボーンにブレンドしない。関節が滑らかに曲がらずパキッと折れるが、これはPS1期の実装そのものであり、意図した挙動である。

初期姿勢はTポーズ（Mixamoの標準）。

### 4.5 PS1風の描画表現

three.js の `MeshBasicMaterial` に `onBeforeCompile` でシェーダーを注入して実装する。ShaderMaterialを自前で書くと、three.jsが提供するスキニング処理を再実装することになるため採らない。

| 表現 | 実装 |
|---|---|
| 頂点ジッター | 頂点をクリップ空間で320×240相当のグリッドにスナップ。動かすと座標が量子化されプルプル揺れる |
| アフィンテクスチャマッピング | 頂点シェーダでUVに `gl_Position.w` を乗算し、フラグメントシェーダで割り戻す。透視補正が打ち消され、面が傾くとテクスチャがぐにゃりと歪む |
| テクスチャフィルタ | ニアレストネイバー、ミップマップなし |
| ライティング | **陰影は頂点カラーに焼き込む**。実行時のライトは既定でオフ（`KHR_materials_unlit` として書き出される） |
| 描画解像度 | 内部を低解像度でレンダリングし、`image-rendering: pixelated` で拡大表示（等倍 / 1/2 / 1/4） |

各表現はUIから個別にオン/オフできる。

描画倍率は**CSSピクセル基準**とする。`devicePixelRatio` を掛けると、高DPI環境で「1/2」がretinaを打ち消すだけになり粗さが出ない。

### 陰影の焼き込み

主光源のランバート項、面の上向き度、高さの3つを掛け合わせた値を頂点カラーに書き込む。PS1期の頂点ライティングと同じ方式。

これにより下を向いた面が暗くなり、**腰の裾や膝の段差の裏側に自然な陰の帯ができて関節が読める**ようになる。

> **テクスチャ側にグラデーションを入れる方法は採れない。** 各パーツは自分のバウンディングボックスを独立してUV領域全体へ投影するため、腿の下端とスネの上端が同じ領域の別の場所を参照してしまい、関節で必ず継ぎ目ができる。頂点カラーは面の向きと世界座標だけで決まるため、別パーツ同士が接していても値が連続する。

焼き込み済みのため実行時のライティングは既定でオフ。オンにすると陰影が二重に掛かって暗くなる。

PNG書き出しも同じ倍率で描いてから拡大するため、プレビューで見えている粗さがそのまま出力される。

### 4.6 エクスポート

**`.glb`（3Dモデル）**

- `GLTFExporter` を `binary: true` で使用し、テクスチャを埋め込んだ単一ファイルとして出力
- スキン情報・ボーン階層を含める
- ファイル名: `<体型ID>-<タイムスタンプ>.glb`

**`.png`（アイコン用）**

- プレビュー中のcanvasを正方形で書き出す
- 背景透過の有無を選択できる

---

## 5. データ形式

### 5.1 体型スペック JSON

**単位はすべて「身長を1としたときの比率」**。メートルではない。`height` を変えても全体が破綻せず、体型の追加が値のコピー修正だけで済む。

```json
{
  "id": "standard",
  "name": "標準",
  "height": 1.7,
  "palette": { "skin": "#c99b76", "hair": "#3a2b22", "cloth": "#4d5a6b" },
  "proportions": {
    "Head": [0, 0.87, 0]
  },
  "parts": [
    {
      "bone": "Head",
      "size": [0.112, 0.132, 0.118],
      "offset": [0, 0.064, 0],
      "uvSet": "head"
    },
    {
      "bone": "LeftArm",
      "size": [0.159, 0.062, 0.062],
      "offset": [0.075, 0, 0],
      "taperAxis": "x",
      "taper": [1.0, 0.88],
      "uvSet": "cloth",
      "mirror": true
    }
  ]
}
```

| フィールド | 意味 |
|---|---|
| `height` | 身長（メートル）。この値だけが実寸 |
| `palette` | 肌・髪・服の色。ベーステクスチャの塗り分けに使う |
| `proportions` | ボーンのワールド位置（身長比）の上書き。書いたボーンだけ既定値を置き換える |
| `parts[].bone` | 追従させるボーン。接頭辞なしの短縮名（`Head`）で書く。`mixamorig:Head` も受け付ける |
| `parts[].size` | 幅・高さ・奥行き（身長比） |
| `parts[].offset` | ボーン原点からのパーツ中心のずれ（身長比） |
| `parts[].taperAxis` | テーパーをかける軸。既定は `y` |
| `parts[].taper` | `[負側の端の倍率, 正側の端の倍率]`。テーパー軸以外の2軸に掛かる。既定は `[1, 1]` |
| `parts[].uvSet` | 使用するUV領域セット（`head` / `torso` / `skin` / `cloth`） |
| `parts[].mirror` | `true` でRight側のパーツを自動生成する。`Left` 系ボーンにのみ指定できる |
| `parts[].detail` | このパーツが現れる最小の細かさ。既定は `low`（常に表示） |
| `parts[].rotate` | パーツ中心まわりの回転（度、XYZ順）。親指のように傾けたいときに使う |
| `parts[].shape` | `box`（既定）または `rings` |
| `parts[].axis` | `rings` の積み上げ軸。既定は `y`。腕は `x` |
| `parts[].sides` | `rings` の断面の角数。既定は8 |
| `parts[].profile` | `rings` の輪郭。`[位置, 倍率A, 倍率B]` を軸の負側から正側へ並べる |

`profile` の倍率が効く軸は積み上げ軸によって変わる。

| axis | 倍率A | 倍率B（基準方向） |
|---|---|---|
| `y` | X（幅） | Z（奥行き） |
| `x` | Z（奥行き） | Y（高さ） |
| `z` | Y（高さ） | X（幅） |

`axis: "x"` のパーツを `mirror` すると、左右で向きが反転するため `profile` の並びは自動で逆にされる。

`proportions` も `mirror` と同様に、`Left` 系だけ書けば `Right` 系が自動で補われる。

### 5.2 UVアトラス（256×256）

| 領域 | 座標 (px) | サイズ | 用途 |
|---|---|---|---|
| `face` | (0, 0) | 128×128 | **顔写真の転写先** |
| `headBack` | (128, 0) | 64×64 | 後頭部 |
| `headSide` | (192, 0) | 64×64 | 側頭部（左右で共用） |
| `headTop` | (128, 64) | 64×64 | 頭頂 |
| `headBottom` | (192, 64) | 64×64 | 顎下 |
| `torsoFront` | (0, 128) | 64×96 | 服（前） |
| `torsoBack` | (64, 128) | 64×96 | 服（後） |
| `torsoSide` | (128, 128) | 32×96 | 服（横） |
| `sleeve` | (160, 128) | 32×96 | 袖 |
| `skin` | (192, 128) | 32×96 | 手・前腕・首・耳 |
| `legs` | (224, 128) | 32×96 | ズボン |
| `shoe` | (0, 224) | 64×32 | 靴 |

顔以外の領域には `palette` に基づくベーステクスチャを敷く。

### 利用者が差し替えられる枠

顔以外に、服・ズボン・靴へ画像を貼れる。

| 枠 | 対象領域（先頭が正面） |
|---|---|
| 服 | `torsoFront` / `torsoBack` / `torsoSide` / `sleeve` |
| 服（背面） | `torsoBack` |
| 髪・後頭部 | `headBack` / `headSide` / `headTop` |
| ズボン | `legs` |
| 靴 | `shoe` |

服（背面）と髪は服より**後に**塗る。同じ領域を上書きする層として働き、正面の写真だけを貼った状態から段階的に足せる。

### 領域ごとの元画像の窓

1枚の写真を全領域へ同じように貼ると、袖のような縦長の領域には**写真の中央が縦一本に切り出される**。胸元の写真ならそこはネクタイであり、肩にネクタイの柄が出てしまう。

正面写真では腕は左右の端に写っているので、袖と側面には端を使う。

| 領域 | 元画像のどこを使うか |
|---|---|
| `torsoFront` / `torsoBack` | 中央68% |
| `torsoSide` / `sleeve` | 左端16% |
| `headBack` | 中央64% |
| `headSide` | 左端18% |
| `headTop` | 上端35%の中央64% |

フリーハンド（`contain`）では窓を使わず画像全体を貼る。切り抜いた形を欠けさせないため。

### テクスチャ解像度

領域の座標は256pxの基準グリッドで定義し、実際の解像度は倍率（1 / 2 / 4倍 = 256 / 512 / 1024px）を掛けて決める。

**実写を貼る場合、256pxの胴（64×96px）では柄が潰れて読めない。** 一方でPS1らしさは低解像度から来ているため、倍率は利用者が選ぶ。実写を使うなら512pxが分岐点。

### 写真からの範囲選択

1枚の写真を読み込み、**使いたい範囲を囲って**貼る。枠によって選べる方法が変わる。

**顔**

| 選び方 | 貼り方 |
|---|---|
| 自動 | 顔検出の結果をもとに目の位置へ合わせる（`paintFace`）。検出できた場合の既定 |
| 矩形 | 囲った範囲を位置合わせなしで顔領域へ（`paintFaceCrop`、`cover`） |

顔検出は範囲選択画面を開く**前**に済ませ、結果をそのまま画面へ渡す。検出できなければ自動は選択不可にし、矩形へ倒す。自動モードでは検出した目の位置に印を描く。

**検出結果は元画像の性質なので、どのモードを選んでも保持する。** 捨てると開き直したときに自動へ戻せなくなる。

**服・ズボン・靴**

| 選び方 | 意味 | fit | 対象領域 |
|---|---|---|---|
| 矩形 | この範囲を生地として使う | `cover`（縦横比を保って埋める） | 全領域 |
| フリーハンド | この形を貼る | `contain`（縦横比を保って収める） | **正面のみ** |

フリーハンドは囲った形の外を透明にし、下地の色を透かす。正面のみに限るのは、ロゴが袖や背中にも出るのを避けるため。

どちらの場合も貼る前に下地を塗る。透明部分から背後の描画が透けるのを防ぐため。矩形で選んだ顔も外周を楕円状に溶かす。顔テクスチャは頭の斜め前の面にも回り込むため、切り抜かないと画像の縁が頭の側面に貼り付く。

元画像と選択状態は保持し、枠を押し直すと**同じ写真のまま選び直せる**。

### 服の種類

上半身5種（タンクトップ / Tシャツ / 長袖シャツ / パーカー / ジャケット）、下半身3種（長ズボン / 半ズボン / スカート）。定義は `src/clothing.js`。

パーツに `id` を与え、`buildBody` の `overrides` で差し替える。体型スペック側を触らずに済む。

| 差し替えられるもの | 用途 |
|---|---|
| `uvSet` | 袖を肌にする、スカートにズボンの領域を使う |
| `scale` | `size` への倍率。パーカーの胴を太くするなど |
| `offsetDelta` | `offset` への加算。ジャケットの着丈を下げるなど |
| `profile` | 輪郭そのもの。スカートの裾を広げる |

**形は倍率と加算で指定し、絶対値では書かない。** `size` / `offset` は身長比で体型ごとに違うため、絶対値だと体型の数だけ用意することになる。

袖丈は独立した設定にしていない。タンクトップの袖丈が意味を持たないため、種類の側に含めてある。

### uvGroup — パーツをまたぐUVの連続

胴は胸・腹・骨盤の3パーツ、脚は腿・膝下の2パーツに分かれている。各パーツが**自分のバウンディングボックスを独立してUV領域全体へ投影する**ため、そのままでは服の画像が胴に3回、ズボンの画像が脚に2回繰り返されてしまう。

パーツに `uvGroup` を指定すると、UVの正規化にグループ全体の範囲を使う。これにより画像がパーツをまたいで連続する。

左右は自動的に別グループになる。同じにすると1枚の画像を左右の脚で分け合い、片脚ずつ違う柄になってしまうため。

---

## 6. 画面仕様

単一画面。左に3Dプレビュー、右に操作パネル。

**プレビュー領域**

- 3Dモデルをリアルタイム表示。ドラッグで回転、ホイールでズーム

**操作パネル**

1. **画像** — 顔・服・ズボン・靴の4枠。**顔も服も同じ「枠に貼る」操作にそろえる**
2. キャラクター — 体型、袖丈、ポーズ
3. 見た目 — PS1表現の各項目のオン/オフ、メッシュの細かさ、描画解像度
4. 書き出し — `.glb` / `.png`

画像を最初に置く。このアプリで最初にやることは画像を貼ることであり、顔だけを特別扱いする理由はない。

**画像の枠（スロットタイル）**

各枠はタイル1つで、サムネイル・名前・状態を持つ。サムネイルには**アトラスの該当領域をそのまま映す**ため、貼った結果がその場で分かる。

- タップ / クリックでファイル選択
- タイルへのドラッグ&ドロップ
- ステージへのドロップは顔として扱う
- 右上の × で外す（画像がある枠にのみ現れる）

顔の枠だけ、貼る前に顔検出を通す。

### モバイル対応

| 項目 | 対応 |
|---|---|
| レイアウト | 900px以下で上下2段（ステージ42dvh / パネルがスクロール） |
| セーフエリア | `env(safe-area-inset-*)` をトークン化し、パネルとヒントの余白に加算 |
| タップ領域 | 操作要素は最小44px。チェックボックスは `<label>` 全体を対象にする |
| 横スクロール | `html`/`body` に `overflow-x: clip`（`hidden` はスクロール文脈を作るため使わない） |
| スロットの列 | `minmax(min(132px, 100%), 1fr)` で320px幅でも2列を保つ |
| 慣性スクロール | パネルに `overscroll-behavior: contain` |

### ポーズ

ボーンの回転（度）で定義する。初期姿勢では全ボーンの回転が0でボーンのローカル軸が世界の軸と一致するため、「Z軸まわりに-70度で腕が下がる」といった直感的な指定がそのまま使える。

Left系だけ書けばRight系はY・Z軸の符号を反転して自動生成する。歩きや手を振るような左右非対称のポーズはRight系も明示する。

搭載（15種）: Tポーズ / 立ち / 歩き / 座り / 手を振る / 走り / ジャンプ / しゃがみ / 腕組み / 考える / 指差す / 万歳 / 敬礼 / 腰に手 / お辞儀

ポーズは `.glb` にもボーンの姿勢として書き出される。**Mixamoのアニメーションを当てる場合はTポーズで書き出すこと。**

**状態遷移**

```
初期表示（体型のみのモデルを表示）
  → 写真選択 → 検出中 → 検出成功: 顔を貼ったモデルを表示
                       → 検出失敗: エラー表示、初期状態を維持
```

写真がなくても体型モデルは常に表示され、書き出しも可能とする。

---

## 7. ディレクトリ構成

```
low-poly-builder/
├── index.html            # importmap定義とマークアップ
├── styles.css
├── tokens.css            # 配色・寸法のデザイントークン
├── src/
│   ├── main.js           # 起動、画面の組み立て、状態管理
│   ├── skeleton.js       # Mixamo標準スケルトンの生成
│   ├── body.js           # 体型スペック → SkinnedMesh
│   ├── atlas.js          # UVアトラスの定義とベーステクスチャ描画
│   ├── face.js           # 写真 → 顔検出 → 位置合わせ → アトラス転写
│   ├── psx.js            # PS1風マテリアル
│   ├── viewer.js         # three.jsのシーン・カメラ・操作
│   └── exporter.js       # .glb / .png 書き出し
├── bodies/
│   ├── index.json        # 体型の一覧
│   ├── standard.json
│   ├── heavy.json
│   ├── slim.json
│   └── chibi.json
├── vendor/               # フェーズ4でthree.js / MediaPipeを取り込む
├── .github/workflows/
│   └── pages.yml         # GitHub Pagesへのデプロイ
├── SPEC.md
└── README.md
```

---

## 8. 非機能要件

| 項目 | 要件 |
|---|---|
| プライバシー | 写真を含む一切のデータを外部送信しない。処理は全てブラウザ内で完結 |
| 対応ブラウザ | Chrome / Edge / Safari / Firefox の最新版（importmapとWebGL2が前提） |
| 描画性能 | プレビューは60fpsを維持 |
| 処理時間 | 顔検出からモデル反映まで3秒以内 |
| モデル規模 | 既定（標準）で1キャラ976ポリゴン、テクスチャ256×256（PS1期の実機水準に合わせる）。細かさの設定で最大2,944まで上げられる |
| オフライン | 体型の生成・プレビュー・書き出しは完全にオフラインで動作。顔検出のみ初回にMediaPipeの取得が必要 |

---

## 9. 開発フェーズ

### フェーズ1: 土台

- `index.html` / importmap / three.jsの読み込み
- プレビュー用のシーン・カメラ・カメラ操作
- GitHub Pagesへのデプロイ

**完了条件:** 公開URLでプレースホルダの立方体が表示され、マウスで回せる

### フェーズ2: ボディ生成とBlender往復の検証

- Mixamo標準スケルトンの生成
- 体型スペックJSON → SkinnedMesh
- 体型4種
- `.glb` 書き出し

**完了条件:** 書き出した `.glb` をBlenderにインポートし、メッシュとボーン階層が正しく読める。Mixamoのアニメーションが適用できる

> **このフェーズを顔の実装より先に置く。** ここが通らないと最終ゴール（Blenderで使う）に届かないため、最大のリスクを先に潰す。

### フェーズ3: 顔パイプライン

- MediaPipe FaceLandmarkerの組み込み
- 写真アップロードUI
- 位置合わせと減色、アトラスへの転写

**完了条件:** 自分の写真から顔の乗ったモデルが生成され、`.glb` にテクスチャが埋め込まれる

### フェーズ4: 仕上げ

- PS1風シェーダー
- UI整形
- 依存のvendor化
- 体型追加の手順をREADMEに記載

**完了条件:** 頂点ジッター・アフィン歪み・低解像度描画が個別に切り替えられ、three.jsがvendor配下から読まれる

---

## 10. 将来拡張

- 体型の追加（JSON追加のみで対応可能な設計になっている）
- パーツ形状の種類追加（円柱、任意の多角柱）
- 服・肌のカラーバリエーション
- 髪型パーツ
- プレビュー内でのアニメーション再生（Mixamoのファイル読み込み）
- 横顔写真の併用による側頭部テクスチャの生成

---

## 11. 決定事項ログ

| 決定 | 選択 | 却下した案 |
|---|---|---|
| 顔テクスチャ | 写真をそのまま転写 | AI生成による描き直し（狙いの質感が消える、コストが発生する） |
| 体型メッシュ | スペックJSONからプロシージャル生成 | 完成済み `.glb` の読み込み（体型追加の手間が大きい） |
| スキニング | 1頂点1ボーンの剛体 | スムーススキニング（PS1の質感から外れる） |
| ビルドツール | なし（importmap） | Vite（今回の依存数では利点が出ない。移行はいつでも可能） |
| UIフレームワーク | なし | React（DOMの複雑さがなく利点が効かない） |
| 実行場所 | 完全にクライアントサイド | サーバー処理（不要かつプライバシー上の不利） |
